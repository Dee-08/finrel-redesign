import {mkdir,writeFile,rm} from 'node:fs/promises';
import {company,services,statistics} from './content.mjs';
import {shell,image,link,arrow,siteOrigin,esc} from './components.mjs';
import {homeRest,otherPages} from './pages.mjs';
export function home(){return `<section class="home-hero"><div class="hero-images">${image('SAM_1373-1210x423.jpg','A pharmacist welcomes customers at Finrel','hero-pharmacy',true)}${image('IMG_2246-1500x1000.jpg','Finrel’s pharmacy team in Ibadan','hero-team',true)}</div><div class="hero-content"><p class="hero-kicker">FINREL NIGERIA LIMITED · IBADAN, SINCE 2015</p><h1>Here for your<br><em>everyday.</em></h1><div class="hero-bottom"><p>From feeling better to living well.<br>Pharmacy, veterinary care, supermarket<br>and bakery — all part of your Finrel.</p><div>${link('Find your nearest Finrel','/branches/','button button-light')}${link('Shop online',company.shop,'hero-shop')}</div></div></div><span class="hero-index">01 — CARE, CLOSE TO HOME</span></section><section class="quick-access" aria-label="Explore our services">${services.map((s,i)=>`<a href="${s.id==='veterinary'?'/veterinary/':'/services/#'+s.id}"><span class="small-label">0${i+1} / ${s.name}</span><span>${s.tag}</span>${arrow}</a>`).join('')}</section><section class="intro section-pad"><div><span class="small-label">ONE FINREL. SO MUCH MORE.</span><h2>For your health.<br>For your home.<br><span class="muted">For the little things.</span></h2></div><div class="intro-copy"><p>Good care belongs in everyday life. A conversation with a pharmacist. Your weekly shop. Fresh bread on the way home. A helping hand for your pet.</p><p>We bring them together, with the warmth and attention you expect from a business in your community.</p>${link('Get to know Finrel','/about/')}</div></section>`;}
const pages=[{path:'/',title:'Pharmacy, Veterinary & Everyday Shopping in Ibadan',description:'Visit Finrel in Ibadan for pharmacy support, veterinary care, supermarket shopping and in-store bakeries. Find a branch or shop online.',body:home()}];
pages[0].body+=homeRest();
pages.push(...otherPages);
for(const p of pages){const dir='dist'+p.path;await mkdir(dir,{recursive:true});let html=shell(p);if(siteOrigin){const url=esc(siteOrigin+p.path);html=html.replace('</head>',`<link rel="canonical" href="${url}"><meta property="og:url" content="${url}"></head>`);}await writeFile(dir+'index.html',html);}
await writeFile('dist/404.html',shell({path:'/404/',title:'Page Not Found',description:'Find your way back to Finrel.',body:'<section class="section-pad not-found"><p>404 / PAGE NOT FOUND</p><h1>Let’s get you<br>back to Finrel.</h1><p>The page you are looking for may have moved.</p>'+link('Return home','/','button')+' '+link('Find a branch','/branches/')+'</section>'}));
await writeFile('dist/robots.txt','User-agent: *\nAllow: /\n'+(siteOrigin?'Sitemap: '+siteOrigin+'/sitemap.xml\n':''));
if(siteOrigin){await writeFile('dist/sitemap.xml','<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+pages.map(p=>`<url><loc>${esc(siteOrigin+p.path)}</loc></url>`).join('')+'</urlset>');}else{await rm('dist/sitemap.xml',{force:true});}
console.log('Built '+pages.length+' page(s).');
